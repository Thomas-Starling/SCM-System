# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SCM System' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SCM System
  pod 'Firebase/Core'

  target 'SCM SystemTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SCM SystemUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
